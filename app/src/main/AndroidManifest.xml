<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thiagohenry.tcc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="Angus"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--Venda-->
        <activity android:name=".VendaCreateActivity"
            android:label="Novo Pedido">

        </activity>

        <activity android:name=".VendaListaActivity"
            android:label="Vendas">

        </activity>
        <!--Produtos-->
        <activity android:name=".ProdutoCreateActivity"
            android:label="Novo Produto">

        </activity>

        <activity android:name=".ProdutoListaActivity"
            android:label="Produtos">

        </activity>
        <!--Clientes-->
        <activity android:name=".ClienteCreateActivity"
            android:label="Novo Cliente">

        </activity>

        <activity android:name=".ClienteListaActivity"
            android:label="Clientes">

        </activity>
    </application>

</manifest>